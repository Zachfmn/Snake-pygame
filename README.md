# Snake-pygame
Make a simple snake game in python to practise pygame and database creation.

Pygame is needed! (pip install pygame)

Run Schema.sql against a fresh SQLite database to recreate required tables.
